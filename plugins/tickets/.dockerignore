# Keep the build context small and reproducible. CI checks out only tracked
# files, but local `docker build` would otherwise ship these into the daemon.
/target
*.db
*.db-wal
*.db-shm
.env
.env.local
